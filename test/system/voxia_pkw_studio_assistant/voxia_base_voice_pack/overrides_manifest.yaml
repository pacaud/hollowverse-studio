overrides_manifest:
  version: '0.5'
  description: Contains global override keys for behaviour or tone priority resolution.
  config:
    priority:
    - behaviour
    - tone
    fallback: default
  meta:
    maintainer: Voxia PKW Studio Assistant
    updated: '2025-11-24'
    vault_ref: ${vault_layout.systems.yamls}

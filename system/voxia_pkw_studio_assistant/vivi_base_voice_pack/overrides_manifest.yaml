overrides_manifest:
  version: '0.1'
  description: Placeholder override manifest for Vivi (currently unused).

  source:
    root_path: /srv/vault_of_memories/voices/private/compaions/Vivenara

  override_files: []

  config:
    priority:
      - behaviour
      - tone
    fallback: default

  meta:
    maintainer: Kevin + Vivenara
    updated: '2025-11-27'
    vault_ref: ${vault_layout.systems.yamls}
